{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "installCommand": "npm install && git clone -b 3.16.0 https://github.com/flutter/flutter.git /tmp/flutter && /tmp/flutter/bin/flutter precache && /tmp/flutter/bin/flutter doctor && /tmp/flutter/bin/flutter build web --web-renderer html --base-href /",
        "buildCommand": "" 
      }
    }
  ],
  
  "outputDirectory": "build/web", 
  
  "routes": [
    {
      "src": "/",
      "dest": "/index.html"
    },
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ],
  "framework": "nextjs", 
  
  "build": {
    "env": {
      "FLUTTER_VERSION": "3.16.0", 
      "FLUTTER_WEB_ENABLE_SUBDIRECTORY_NAVIGATION": "true" 
    }
  }
}
